build:
  gpu: true
  python_version: "3.11"
  system_packages:
    - "ffmpeg"
  python_packages:
    - "whisply"
  run:
    # Create cache directory for models
    - mkdir -p /root/.cache/huggingface
    # Cache models by running whisply on a test file
    - whisply --model large-v3 --device gpu test/dummy.mp3
    - whisply --model distil-large-v3 --device gpu test/dummy.mp3
    - whisply --model large-v3-turbo --device gpu test/dummy.mp3
    # Clean up output files
    - rm -rf output/

predict: "app.py:Predictor"
